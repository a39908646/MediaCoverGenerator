# Archive

这里存放当前独立版服务运行**不再依赖**、但仍保留以便回溯或最终删除的内容。

建议分两类看：

- `legacy-moviepilot/`：旧 MoviePilot 插件实现和配套元数据
- `development/`：开发期测试或临时辅助内容

当前服务运行所需的核心内容仍然在仓库根目录：

- `mediacovergenerator/`
- `data/`
- `fonts/`
- `images/`
- `compose.yaml`
- `Dockerfile`
- `requirements.txt`
